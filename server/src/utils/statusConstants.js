/**
 * Status Constants
 * Single source of truth for all feedback status values.
 * Import this everywhere instead of hardcoding strings.
 */

const STATUS = {
    NEW: 'New',
    IN_REVIEW: 'In Review',
    BEING_RESOLVED: 'Being Resolved',
    RESOLVED: 'Resolved',
    CLOSED: 'Closed',
};

export default STATUS;
